# بنك الدم المركزي — إصدار Workflow v2

## ما تم تطويره
- إغلاق تصعيد الصلاحيات: لا يمكن للمستخدم تعديل `role` أو `hospital_id` أو `is_active` من المتصفح.
- عمليات المخزون الحساسة عبر PostgreSQL RPC بصلاحيات مدققة.
- منع حذف وحدات الدم؛ الإتلاف يحول الحالة إلى `discarded`.
- FEFO حقيقي: الحجز الذري يختار الوحدات الأقرب انتهاءً أولاً.
- منع حجز/تسليم الدم المنتهي.
- ربط الوحدات المحجوزة بالطلب نفسه (`reserved_for_request_id`) لمنع تحرير/تسليم وحدات تخص طلباً آخر.
- دورة الطلب ذرية: مراجعة → قبول → حجز → تجهيز → تسليم، مع إلغاء وإرجاع الوحدات عند الحاجة.
- منع إنشاء طلبات برقم مكرر؛ أرقام الطلبات والتسليم والوحدات يولدها PostgreSQL.
- سجل تدقيق غير قابل للتعديل أو الحذف للعمليات الأساسية.
- إشعارات تلقائية من قاعدة البيانات.
- Realtime: عند وصول طلب جديد يظهر تنبيه منبثق في واجهة المستشفى المورّد مع صوت ومحاولة انتقال مباشر إلى صفحة الطلبات.
- طباعة تقرير الطلب وتصديره CSV.
- تقارير قابلة للطباعة والتصدير.
- انتهاء الصلاحية والتنبيهات القريبة من الانتهاء.
- تفعيل/تعطيل حسابات الموظفين مع إبطال جلسة الحساب المعطل.
- Forgot Password / تغيير كلمة المرور.
- شعار وصورة المستشفى، وصورة الملف الشخصي.
- إعدادات وهوية المستشفى.
- تحديث تلقائي لحالة المستشفى الموقوف أثناء الجلسة.

## التثبيت في Supabase
1. ارفع مجلد `Bank-main` إلى GitHub أو افتحه محلياً.
2. أنشئ/افتح مشروع Supabase.
3. طبّق migrations بالترتيب الموجود داخل:
   `supabase/migrations/`
4. انشر Edge Function:
   `supabase/functions/create-user/index.ts`
5. ضع في `.env`:
   `VITE_SUPABASE_URL`
   `VITE_SUPABASE_ANON_KEY`
6. في Supabase Auth > URL Configuration أضف عنوان الموقع النهائي في Site URL وRedirect URLs حتى يعمل Forgot Password.
7. فعّل Realtime إذا كان مشروعك يتطلب تفعيل الجداول من لوحة Supabase؛ migration تضيف `notifications` و`blood_requests` إلى publication.

## التشغيل
```bash
npm install
npm run dev
```

## البناء
```bash
npm run typecheck
npm run build
```

## ملاحظة مهمة
كل مستشفى يرى مخزونه التفصيلي فقط. شاشة لوحة التحكم تعرض التوفر المركزي كمجاميع بدون كشف وحدات المستشفيات الأخرى. عند إرسال طلب إلى مستشفى مورّد، تصل إليه الإشعارات والطلب عبر RLS + Realtime.

### تنبيه الصوت
المتصفحات قد تمنع الصوت التلقائي قبل أول تفاعل مع الصفحة. بعد تفاعل المستخدم مع النظام يمكن تشغيل نغمة التنبيه؛ المنبثق نفسه يظهر حتى لو منع المتصفح الصوت.

## ترقية سير طلب الدم ولوحة المخزون — 2026-09-09

تمت إضافة ترقية جديدة في:
`supabase/migrations/20260909123000_request_routing_dashboard.sql`

### ما الذي تغير؟
- عند إنشاء طلب الدم، يظهر فقط المستشفى النشط الذي يملك الكمية المطلوبة فعلياً من الفصيلة والمكوّن.
- إشعار الطلب الجديد يذهب إلى المستشفى المورّد فقط، وليس إلى المستشفى الذي أنشأ الطلب.
- شاشة التنبيه الفورية والصوت الموجودة مسبقاً تستقبل طلبات المستشفى المورّد.
- أي موظف في المستشفى المورّد يستطيع قبول الطلب.
- القبول أصبح عملية ذرية واحدة: قبول + حجز الكمية تلقائياً باستخدام FEFO ومنع الحجز الجزئي.
- تم ربط الحجز بالطلب نفسه عبر `reserved_for_request_id` لمنع استخدام وحدات محجوزة لطلب آخر.
- عند التجهيز ثم التسليم، تُخرج الوحدات المحجوزة من المخزون وتُسجل هوية منفذ العملية.
- تم تسجيل منشئ الطلب ومنفذ القبول والتجهيز والتسليم في `blood_requests`.
- صلاحيات عرض الطلبات في الواجهة أصبحت حسب الدور: المدير العام، مدير المستشفى، والموظف حسب العمليات التي تخصه/نفذها مع إبقاء الطلبات الجديدة الواردة لموظفي المستشفى المورّد.
- تم الحفاظ على الطباعة وتصدير CSV والتحديث اللحظي وسجل العمليات والتنبيهات الحالية.
- لوحة التحكم تعرض دائماً مخططين لحالة المخزون لكل فصيلة:
  1. النظام بالكامل.
  2. مستشفى المستخدم، أو مستشفى يختاره المدير العام.
- المخططات تعرض الحالات: متاح، محجوز، مُسلّم، منتهي، مُتلف.
- الصورة الشخصية في الإعدادات تحفظ كـ Base64 داخل قاعدة البيانات، بينما تبقى هوية المستشفى (الشعار/الصورة) على Storage.
